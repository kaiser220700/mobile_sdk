name: mobile_app_base
description: Shared Flutter app shell and host runtime configuration for the mobile SDK examples.
publish_to: none
version: 0.2.0

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
