name: mobile_update
description: Reusable Flutter update policy helper for force and soft app updates.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.12.0 <4.0.0"

dev_dependencies:
  lints: ^6.0.0

